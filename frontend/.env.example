# 后端 API 基础路径
NEXT_PUBLIC_API_BASE=http://localhost:8080/api/v1

# 站点前台 URL（用于 SEO / canonical / OpenGraph）
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# /api/revalidate 使用的秘密（如启用手动重新验证）
REVALIDATE_SECRET=change-me-in-prod

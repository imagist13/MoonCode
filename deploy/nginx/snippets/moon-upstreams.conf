# moon-upstreams.conf
# 反代上游定义（moon.conf / moon-ssl.conf 共用，避免配置分裂）

upstream moon_backend {
    server 127.0.0.1:8080;
    keepalive 32;
}

upstream moon_frontend {
    server 127.0.0.1:3000;
    keepalive 16;
}